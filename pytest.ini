[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Run API tests by default; use -m e2e for E2E or run tests/e2e/
markers =
    e2e: end-to-end browser tests (require server and playwright)
    api: API integration tests (require server)
